<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- The system's light or dark, with no action bar: the page draws all of
         the chrome. -->
    <style name="Ledge" parent="@android:style/Theme.DeviceDefault.DayNight">
        <item name="android:windowActionBar">false</item>
        <item name="android:windowNoTitle">true</item>
    </style>
</resources>
